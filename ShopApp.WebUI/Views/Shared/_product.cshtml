@model Product

@if(Model != null)
{
<div class="card">
    <img src="~/img/@Model.ImageUrl" alt="" class="card-img-top">
    <div class="card-body">
    <h5 class="card-title">@Model.Name</h5>
    <p class="card-text">Fiyat :@Model.Price</p>
    
    <a asp-controller="Product" asp-action="Details" asp-route-id="@Model.Id" class="btn btn-primary">İncele</a>
    
    </div>
</div>
}
else{
   
    @await Html.PartialAsync("_noproduct")
}
